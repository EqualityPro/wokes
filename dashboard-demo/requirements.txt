# Flask + its deps are all PURE PYTHON (no Rust, no C compiler needed),
# so this installs instantly on Termux/Android and locked hosts.
# (FastAPI was dropped because pydantic-core needs Rust, which fails on Termux.)
flask
